# Vite dev/preview proxies /api and /health to this origin (see vite.config.ts).
# Default is http://localhost:8000 (local uvicorn).
# If the API runs via Docker Compose on host port 8100, copy to .env.local and set:
# VITE_DEV_PROXY_TARGET=http://localhost:8100
